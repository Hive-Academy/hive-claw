# Reduce build context — only Dockerfile/entrypoint/template are needed at build time.
.git/
.gitignore
.env
.env.example
README.md
LICENSE
docs/
skills/
commands/
templates/
bin/
setup.sh
docker-compose.yml
log.md
*.md
.vscode/
.claude/
.ptah/
.mcp.json
node_modules/

# TASK_2026_006 Batch 7: keep the plugin builder stage's outputs out of the
# build context. The multi-stage `npm ci + npm run build` produces them fresh.
openclaw-control/plugin/node_modules
openclaw-control/plugin/dist
