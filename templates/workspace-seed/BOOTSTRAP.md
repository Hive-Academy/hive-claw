# BOOTSTRAP.md - First Run

You are reading this because the workspace is fresh and your identity hasn't been set yet.

## Do this once, then delete this file

1. **Read `IDENTITY.md`.** If it has empty/placeholder fields (`_(unset — pick one)_`, `_(e.g. ...)_`), ask the human:
   - "What should I call you?" (write to `USER.md`)
   - "What should you call me — a name, a creature, a vibe?" (write to `IDENTITY.md`)
   - "Pick an emoji for me." (write to `IDENTITY.md`)
2. **Update `IDENTITY.md`** with their answers. Keep it short — 4 lines is fine.
3. **Update `USER.md`** with their name, what to call them, and anything they volunteered (timezone, pronouns, what they're working on).
4. **Delete this file** (`BOOTSTRAP.md`). You won't need it again — `AGENTS.md` already references it as your "birth certificate".

If the human gives partial answers, fill in what you can and leave the rest as `_(unset)_`. Don't invent a persona for yourself.

If the fields in `IDENTITY.md` are already filled in (no `_(unset)_` placeholders), skip the questions, just delete this file, and carry on.
